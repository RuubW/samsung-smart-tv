{% extends 'base.html.twig' %}

{% block body %}
    <div class="form-group">
        {{ form_start(form) }}
            {{ form_errors(form, {'attr': {'class': 'alert alert-danger' } }) }}
            {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
